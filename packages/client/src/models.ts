export type Post = {
    title: string;
    image: string;
    content: string;
    postId: string;
};

export type User = {
    firstName: string;
    lastName: string;
    email: string;
    photo: string
};
